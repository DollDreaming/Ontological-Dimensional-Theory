# 이 저장소에서 일하는 법

이 저장소는 존재론적 차원이론(ODT) 공개 위키다. `docs/`의 마크다운이 곧 위키 페이지이고, `main`에 들어가면 GitHub Actions가 GitHub Pages로 배포한다. 한국어 쪽 `page.md` 옆에 영문 쪽 `page.en.md`가 짝으로 있고, 영문 사이트는 `/en/` 아래에 만들어진다.

## docs/는 저자 원본의 사본이다

- `docs/`의 쪽들은 저자 PC의 위키 원본에서 온다. 저자 PC의 자동 갱신이 하루 한 번, 원본에서 바뀐 쪽만 이 저장소에 올린다.
- 그래서 여기서 `docs/`를 고치면 원본과 어긋난다. 저자가 여기서 고치라고 분명히 할 때만 고치고, 풀 리퀘스트 설명 첫 줄에 "원본에도 반영 필요"라고 적는다. 여기서 고친 쪽은 자동 갱신이 덮어쓰지 않고 저자에게 알린다. 원본에서도 그 쪽이 바뀌면 충돌로 멈춘다.
- `mkdocs.yml`, `requirements.txt`, `docs/stylesheets/`, `docs/javascripts/`, `.github/`, 안내 문서(README · CLAUDE · MAINTAINING)는 이 저장소가 원본이다.

## 싣는 것 — 쪽마다 최신본

- 쪽마다 지금의 내용만 싣는다. "예전에는 …, 지금은 …" 같은 판 대조, 판 번호, "개정 중" 상자를 쓰지 않는다. 지난 내용은 git 이력이 맡는다.
- 싣는 것: 저자의 본편과 그 뒤 저자가 밝힌 이론 내용 — 1~9성의 정의, 핵심 주장, 여러 분야와의 대응, 열린 과제.
- 싣지 않는 것: 저자가 확인하지 않은 형식화(새 계산식 · 새 기호 · 새 조건), 구현 세부, 저자 연락처(이메일 등).
- 저자의 말은 따옴표 안에 글자 그대로 옮기고 날짜를 붙인다: `> "…" — 저자, YYYY-MM-DD`. 물음꼴("…한다면?")은 풀어서 단정하지 않는다. 본편 인용은 인용 끝줄에 `> — 본편 「절 이름」`.
- 본편 주장을 약하게 옮기지 않고, "~라 볼 수 있다" 같은 한정어도 원문대로 둔다.
- 새 말을 만들지 않는다. 용어는 용어집(`docs/glossary.md`)의 뜻 하나로만 쓴다. 특히 성, 위상, 층 · 층위 · 계층, 시간(변화이며 축이 아니다).

## 영문판

- 한국어판이 기준이고 영문판은 번역이다. 한국어 쪽을 고치면 짝 영문 쪽도 맞춘다.
- 영문 쪽 맨 위에 짝 한국어 쪽의 지문을 둔다: `---` / `ko_sha: <한국어 쪽 파일의 sha256 앞 16자>` / `---`. 한국어 쪽이 바뀌었는데 이 값이 옛값이면 영문이 낡은 것이다.
- 구조를 한국어 쪽과 똑같이 둔다: 제목 단계와 순서, 링크 대상, 수식, 표, 상자, 상태 표시, `{ #id }`.
- 저자 인용은 영어로 옮기고 각주에 원문을 붙인다: `> "…" — Author, YYYY-MM-DD[^q1]` … `[^q1]: Original (Korean): "…"`. 본편 인용의 끝줄은 `> — The main paper, "Section"[^q2]`.
- 용어는 영문 용어집(`docs/glossary.en.md`)을 따른다: 성 = dimension, 위상 = phase, 층 = tier, 층위 = layer, 계층 = level.

## 쓰는 방식

- 각 쪽은 굵은 요지 문장으로 시작한다. 문장은 짧게 쓴다.
- 성격이 다른 설명에만 상태 표시를 붙인다. 표시가 없는 문장은 저자의 이론 본문이다. 대응 `st-int`, 차용 `st-bor`, 가설 `st-hyp`, 열림 `st-rev` (영문: Correspondence, Borrowed, Hypothesis, Open). 쓰는 법은 MAINTAINING.md에 있다.
- 상태 표시가 붙은 제목에는 `{ data-toc-label="제목" }`을 단다. 다른 쪽에서 링크하는 제목에는 `{ #id }`를 달아 두 판의 주소를 같게 한다.
- 1~9성 쪽은 정의 → 구조적 선언 → 해석 → 특징과 확장 → (열린 것) → 여러 분야에서의 대응 → 근거 순서를 지킨다.
- 새 쪽은 한국어 · 영문 두 판을 함께 만들고, `mkdocs.yml`의 `nav`에 넣고 영문 메뉴 이름을 `nav_translations`에 더한다.

## 공개 범위

- 저자 표기는 `서원용 (Seo Won-Yong)`, 라이선스는 CC BY-NC-SA 4.0이다.
- 내부 작업 이름, 파일 경로, 연락처를 싣지 않는다.

## 올리기 전

- `pip install -r requirements.txt` 뒤 `mkdocs build --strict`가 경고 없이 통과해야 한다(두 판 모두 만들어진다).
- 사람이 여는 세션은 기본으로 `claude/` 브랜치에 올리고 풀 리퀘스트를 연다. 저자가 분명히 요청할 때만 `main`에 바로 올린다.
- 풀 리퀘스트 설명에는 바꾼 쪽과 근거(본편 절, 저자 발언 날짜 등)를 적는다.

## 자동 갱신 (저자 PC, 하루 한 번)

- 원본에서 바뀐 쪽만 `docs/`로 복사해 `main`에 올린다. 게시 방식은 저자 결정(2026-09-24)에 따라 검사를 통과하면 바로 게시다.
- 한국어 쪽이 바뀌었는데 영문 짝이 그대로면 그 쪽만 Claude Code가 영어로 옮긴다. 번역이 끝나기 전에는 영문 쪽 위에 "Translation in progress" 상자가 붙는다.
- 게시 전 검사: 저자 인용이 저자 원문 기록에 글자 그대로 있나 · 영문 짝의 구조가 한국어 쪽과 같나 · 내부 이름 · 경로 · 연락처가 없나 · `mkdocs build --strict` · 변경이 너무 크지 않나. 걸린 쪽은 올리지 않는다.
- 쪽을 새로 만들거나 지우는 일은 메뉴를 사람이 정해야 해서 자동으로 하지 않는다.
- 빌드가 깨지거나 변경이 너무 크면 게시하지 않고 `claude/wiki-sync-hold` 브랜치에 둔다.
