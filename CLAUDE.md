# 이 저장소에서 일하는 법

이 저장소는 존재론적 차원이론(ODT) 공개 위키의 원본이다. `docs/`의 마크다운이 곧 위키 페이지이고, `main`에 들어가면 GitHub Actions가 GitHub Pages로 배포한다.

## 기준

- 기준 판본은 본편 v9.4다. 정의·수식·인용은 본편과 대조해 그대로 옮긴다. `— 본편 v9.4 …`로 끝나는 인용 블록은 글자 그대로다.
- 본편과 다르게 적은 곳(기호 정리, 오탈자 교정 등)은 그 쪽의 `근거` 상자에 이유를 적는다.
- v9.4 이후 저자의 관점은 `!!! warning "현행 읽기 · 개정 중"` 상자 안에만 쓴다. 상자 밖 본문에 섞어 본편 주장처럼 쓰지 않는다.
- 본편 주장을 약하게 옮기지 않고, "~라 볼 수 있다" 같은 한정어도 원문대로 둔다.

## 공개 범위

- 본편 공개판 수준만 싣는다: 1~9성 정의, 핵심 주장, 분야별 대응, 열린 과제.
- 용어 구조와 구현 세부는 싣지 않는다.
- 저자 연락처(이메일 등)는 싣지 않는다. 저자 표기는 `서원용 (Seo Won-Yong)`, 라이선스는 CC BY-NC-SA 4.0이다.

## 쓰는 방식

- 각 쪽은 굵은 요지 문장으로 시작한다. 문장은 짧게 쓴다.
- 문장이나 제목 끝에 상태 표시를 붙인다: 정의 `st-def`, 유도 `st-der`, 해석 `st-int`, 차용 `st-bor`, 가설 `st-hyp`, 개정 중 `st-rev`. 쓰는 법은 README에 있다.
- 상태 표시가 붙은 제목에는 `{ data-toc-label="제목" }`을 달아 목차에 표시어가 섞이지 않게 한다.
- 1~9성 쪽은 정의 → 구조적 선언 → 해석 → 특징과 확장 → 여러 분야에서의 대응 → 근거 순서를 지킨다.
- 새 쪽을 만들면 `mkdocs.yml`의 `nav`에도 넣는다.

## 올리기 전

- `pip install -r requirements.txt` 후 `mkdocs build --strict`가 경고 없이 통과해야 한다.
- 기본은 `claude/` 브랜치에 올리고 풀 리퀘스트를 연다. 저자가 분명히 요청할 때만 `main`에 바로 올린다.
- 풀 리퀘스트 설명에는 바꾼 쪽과 근거(본편 몇 쪽, 저자 요청 등)를 적는다.
